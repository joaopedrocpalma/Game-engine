--Holds sound data
--file -> specifies the file name to search fore
--s_type -> mono if 0, stereo if 1. Stereo sound should only be used for music,
--as it does not support possitional displacement.

audioFileList = {
	sound1 = {
		file = 'windowsXPError',
		s_type = 0, 
	},
	sound2 = {
		file = 'meow',
		s_type = 0, 
	},
	
}